# Stage 0 (current)
chromadb>=1.5.0
pillow>=10.0.0

# Web API + demo UI (lightweight, pure-Python)
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# Stage 1 (real models) — heavy; kept in a separate file so this base set stays
# lightweight and installable everywhere. Install with:
#     pip install -r requirements-stage1.txt
# The engine runs without them: each extractor/embedder falls back to a
# deterministic stub when its dependency (or the Ollama daemon) is unavailable.

# Stage 5 (PDF) — pure-Python, no system deps. pypdfium2 (Apache/BSD) renders
# pages for the vision path; rendering is optional (text-only without it).
pdfplumber>=0.11.0
pypdfium2>=4.30.0

# Dev
pytest>=8.0.0
psutil>=5.9.0              # RAM-aware worker sizing (Stage 4)
